<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark only, by design.  The Compose theme is dark unconditionally, so a
         light platform theme here would flash white before the first frame. -->
    <style name="Theme.LINE_Chrome" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
