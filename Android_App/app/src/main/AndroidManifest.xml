<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Predictive back is on by default from Android 15, which targetSdk 36
         already opts into.  Declaring it explicitly is what extends the gesture
         preview back to Android 13 and 14.  The catch is that on those two it
         also switches back dispatch to the new API, which stops calling
         Activity.onBackPressed() — safe here because nothing overrides it and
         every back in the app goes through BackHandler / PredictiveBackHandler,
         which route themselves either way.  Below Android 13 the flag is
         ignored and back behaves exactly as it did. -->
    <application
        android:name=".LineApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LINE_Chrome"
        tools:targetApi="34">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.LINE_Chrome"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- dataSync would be the closer-sounding fit, but it is budgeted at six
             hours per day on Android 14+ — after which the system stops the
             service and messages simply stop arriving until the app is reopened.
             A messaging connection cannot be part-time, and there is no push
             channel to fall back on because LINE delivers through its own app,
             so specialUse it is. -->
        <service
            android:name=".service.PollingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Holds the long-poll connection that delivers incoming LINE messages. LINE provides no push channel to third-party clients, so the connection must stay open for notifications to arrive at all." />
        </service>

        <!-- Hands a downloaded attachment to an external viewer or player
             without making it world-readable. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.media"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/media_paths" />
        </provider>
    </application>

</manifest>
